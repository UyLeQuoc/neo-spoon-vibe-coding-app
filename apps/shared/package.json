{
  "name": "shared",
  "type": "module",
  "private": true,
  "exports": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "scripts": {
    "dev": "tsdown --watch --no-clean --ignore-watch '.turbo/**'",
    "test": "node --test",
    "check:types": "tsc --noEmit",
    "build": "tsdown"
  },
  "dependencies": {
    "zod": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsdown": "catalog:"
  }
}
