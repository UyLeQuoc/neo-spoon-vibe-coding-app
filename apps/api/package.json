{
  "name": "api",
  "private": true,
  "types": "./dist/index.d.mts",
  "scripts": {
    "dev": "wrangler dev --var 'IS_LOCAL:true'",
    "deploy": "wrangler deploy",
    "gen": "drizzle-kit generate",
    "migrate": "wrangler d1 migrations apply DB",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "build": "tsdown",
    "build:watch": "tsdown --watch --no-clean --ignore-watch '.turbo/**'",
    "check:types": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.44",
    "@ai-sdk/mcp": "^0.0.8",
    "@cityofzion/neon-dappkit": "^1.0.0",
    "@hono/zod-validator": "^0.7.4",
    "@modelcontextprotocol/sdk": "^1.22.0",
    "@openrouter/ai-sdk-provider": "^1.2.3",
    "ai": "^5.0.93",
    "drizzle-orm": "^0.44.7",
    "hono": "catalog:",
    "shared": "workspace:^",
    "tsdown": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "@vitest/ui": "^4.0.9",
    "drizzle-kit": "^0.31.7",
    "vitest": "^4.0.9",
    "wrangler": "catalog:"
  }
}
